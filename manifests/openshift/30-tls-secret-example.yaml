apiVersion: v1
kind: Secret
metadata:
  name: amq-tls-mqtt
  namespace: amq-multicluster
type: Opaque
data:
  # Base64 de un keystore Java (broker.ks) y, opcionalmente, truststore (client.ts) si habilitas mTLS.
  # No se incluyen certs reales en este repo.
  broker.ks: ""
  client.ts: ""
  keyStorePassword: ""
  trustStorePassword: ""

